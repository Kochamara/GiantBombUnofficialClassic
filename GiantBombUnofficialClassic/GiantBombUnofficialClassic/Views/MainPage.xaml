<Page
    x:Class="GiantBombUnofficialClassic.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GiantBombUnofficialClassic.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Enabled"
    xmlns:converters="using:GiantBombUnofficialClassic.Views.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:NegatedBoolToVisibilityConverter x:Key="NegatedBoolToVisibility"/>
    </Page.Resources>

    <ScrollViewer Background="{StaticResource DarkestGrayBrush}">
        <StackPanel Orientation="Vertical">
            <StackPanel Visibility="{Binding ShowJeff, Converter={StaticResource BoolToVisibility}}" Orientation="Vertical">
                <Image Source="ms-appx:///Assets/JeffFrame.jpg" MaxWidth="400" MaxHeight="600" Margin="0,100,0,24"  Stretch="Uniform" HorizontalAlignment="Center" />
                <ProgressRing Style="{StaticResource ProgressRingStyle}" />
            </StackPanel>
            <StackPanel Visibility="{Binding ShowJeff, Converter={StaticResource NegatedBoolToVisibility}}" Orientation="Vertical">
                <Image Source="ms-appx:///Assets/GiantBombLogo.png" MaxWidth="300" MaxHeight="300" Margin="40" Stretch="Uniform" HorizontalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Search" Command="{Binding NavigateSearchPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                    <Button Content="Categories" Command="{Binding NavigateCategoriesPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                    <Button Content="Quick Looks" Command="{Binding NavigateQuickLooksPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                    <Button Content="Settings" Command="{Binding NavigateSettingsPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                </StackPanel>
                <TextBlock Text="Latest Videos" HorizontalAlignment="Center" Margin="20" Style="{StaticResource HeaderTextStyle}" />
                <ItemsControl ItemsSource="{Binding Videos}" Style="{StaticResource VideoListStyle}"  />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>