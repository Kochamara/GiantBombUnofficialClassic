<Page
    x:Class="GiantBombUnofficialClassic.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GiantBombUnofficialClassic.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Enabled"
    xmlns:converters="using:GiantBombUnofficialClassic.Views.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:NegatedBoolToVisibilityConverter x:Key="NegatedBoolToVisibility"/>
    </Page.Resources>

    <ScrollViewer Background="{StaticResource DarkestGrayBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Welcome to Giant Bomb Unofficial Classic" HorizontalAlignment="Center" Margin="40" Style="{StaticResource HeaderTextStyle}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Search" Command="{Binding NavigateSearchPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                <Button Content="Categories" Command="{Binding NavigateCategoriesPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                <Button Content="Quick Looks" Command="{Binding NavigateQuickLooksPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
                <Button Content="Settings" Command="{Binding NavigateSettingsPageCommand}" Style="{StaticResource NavigationButtonStyle}" />
            </StackPanel>

            <Image Source="ms-appx:///Assets/jeff.jpg" MaxWidth="400" MaxHeight="600" Margin="100" Visibility="{Binding ShowJeff, Converter={StaticResource BoolToVisibility}}" Stretch="Uniform" HorizontalAlignment="Center" />
            <ItemsControl ItemsSource="{Binding Videos}" Style="{StaticResource VideoListStyle}" Visibility="{Binding ShowJeff, Converter={StaticResource NegatedBoolToVisibility}}" />
        </StackPanel>
    </ScrollViewer>
</Page>
